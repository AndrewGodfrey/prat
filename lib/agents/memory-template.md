# Memory

This file is read-only, deployed by `d` — never edit it or other `~/.claude/projects/*/memory/`
files directly.

To save a memory (corrections, domain knowledge, references, user facts), invoke the `remember`
skill — it decides placement and format. The target files are in git repos, so edits are visible
and reviewable; no need to wait for confirmation.
