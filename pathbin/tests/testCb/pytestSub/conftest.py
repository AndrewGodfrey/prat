# Fixture only: a pytest marker, so testCb/pytestSub registers as a subproject whose test framework
# is auto-detected as pytest. See pathbin/tests/Test-Codebase.tests.ps1, "*.Tests.ps1 focus inside a
# pytest-only subproject".
