#!/bin/bash
# Wrapper for Test-Project.ps1.
# Enables a single Bash(t *) permission grant rather than ad-hoc pwsh invocations.

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
pwsh -File "$SCRIPT_DIR/Test-Project.ps1" "$@" 2>&1
